import { styled } from 'styled-components';

const InputTab = () => {
  return <Container>hello Worldland</Container>;
};

export default InputTab;

const Container = styled.div`
  display: flex;
  align-items: center;
  color: white;
  z-index: 4;
`;
